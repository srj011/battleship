[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0"
serde = "1.0.228"

[dependencies.uuid]
version = "1.21.0"
# Lets you generate random UUIDs
features = [
    "v4",
]
